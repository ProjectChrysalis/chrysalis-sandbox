{
  "name": "chrysalis-sandbox",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build:busybox": "sh scripts/build-busybox.sh",
    "build:jq": "sh scripts/build-jq.sh",
    "build:python": "bun scripts/build-python.ts",
    "build:quickjs": "bun scripts/build-quickjs.ts",
    "pack": "bun scripts/pack.ts",
    "release": "bun scripts/build-python.ts && bun test && bun scripts/pack.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@jitl/quickjs-ng-wasmfile-release-sync": "^0.32.0",
    "esbuild": "0.28.2",
    "fflate": "^0.8.3",
    "quickjs-emscripten-core": "^0.32.0"
  },
  "license": "GPL-2.0-only"
}
